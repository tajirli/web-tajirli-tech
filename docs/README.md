# Documentation Portal

This folder contains the hosted documentation portal used for architecture and progress tracking.

## Important

- The source material is mirrored from `.internal-docs/`.
- This content is not imported into product runtime routes.
- Keep links relative to `docs/` so GitHub Pages URLs stay valid.

## Open locally

Open `docs/index.html` directly in your browser.
